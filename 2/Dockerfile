# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY, USE /Dockerfile.tmpl.php

# https://hub.docker.com/_/php
FROM php:7.4-alpine

MAINTAINER Instrumentisto Team <developer@instrumentisto.com>


# Install phpDocumentor executable
RUN curl -fL -o /usr/local/bin/phpdoc \
         https://github.com/phpDocumentor/phpDocumentor2/releases/download/v2.9.1/phpDocumentor.phar \
 && chmod 755 /usr/local/bin/phpdoc


# Install required PHP extensions and packages
RUN apk add --update --no-cache \
            graphviz \
            icu-libs libxslt libzip \
            make \
    \
 && apk add --no-cache --virtual .build-deps \
            icu-dev libxslt-dev libzip-dev \
 && docker-php-ext-install intl xsl zip \
    \
 && apk del .build-deps \
 && rm -rf /var/cache/apk/*


VOLUME ["/app"]

WORKDIR /app


ENTRYPOINT ["/usr/local/bin/phpdoc"]
